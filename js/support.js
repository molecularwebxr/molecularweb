var supportsCustomElements = "customElements" in window;
if (!supportsCustomElements) {
  alert(
    "Your browser is not supported. We recommend up-to-date version of Google Chrome for the best interactive experience"
  );
}
